<UserControl x:Class="PowersOfTwo.Views.HighscoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0">
      <StackPanel Orientation="Horizontal">
        <Image Source="../Images/Highscore.png"/>
        <TextBlock Text="Highscores"/>
      </StackPanel>
    </Border>
    
    <ItemsControl Grid.Row="1">
    </ItemsControl>

    <Button Grid.Row="2" Command="{Binding BackToMainMenuCommand}" Margin="20" HorizontalContentAlignment="Stretch">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Margin="4" Source="../Images/Back.png" HorizontalAlignment="Left"/>
        <TextBlock Grid.Column="1" Text="Back to menu" VerticalAlignment="Center" HorizontalAlignment="Center"/>
      </Grid>
    </Button>
    
  </Grid>
</UserControl>
