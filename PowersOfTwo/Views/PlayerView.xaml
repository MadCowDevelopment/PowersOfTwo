<UserControl x:Class="PowersOfTwo.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:PowersOfTwo.Core;assembly=PowersOfTwo.Core"
             xmlns:Controls="clr-namespace:PowersOfTwo.Controls" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Grid.Column="0" CornerRadius="10" BorderBrush="Black" Margin="10" 
            BorderThickness="2" Background="Khaki">
      <TextBlock Text="{Binding Points}" FontSize="30" FontFamily="Courier" HorizontalAlignment="Center"/>
    </Border>
    <Controls:PlayFieldControl Grid.Row="1" Margin="10" DataContext="{Binding Cells}"/>
  </Grid>
</UserControl>
