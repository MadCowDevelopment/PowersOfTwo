<UserControl x:Class="PowersOfTwo.Controls.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="70"
             d:DesignHeight="300" d:DesignWidth="300">

  <Button Margin="20,10,20,10"
          Grid.Row="0" Command="{Binding Path=Command, RelativeSource={RelativeSource AncestorType=UserControl}}" 
          HorizontalContentAlignment="Stretch">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Image Grid.Column="0" Margin="4" HorizontalAlignment="Center"
             Source="{Binding Image, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
      <TextBlock Grid.Column="1" Margin="10,0,0,0" 
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                 VerticalAlignment="Center" HorizontalAlignment="Left"/>
    </Grid>
  </Button>
</UserControl>
